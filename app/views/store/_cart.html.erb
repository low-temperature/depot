<div class="cart-title">カート</div>
<table>
  <%= render(:partial => "cart_item", :collection => cart.items) %>

  <tr class="total-line">
  	<td colcpan="2">合計</td>
  	<td class="toral-cell"><%= number_to_currency(@cart.total_price) %></td>
  </tr>
</table>

<%= button_to "カートを空にする", :action => :empty_cart %>

